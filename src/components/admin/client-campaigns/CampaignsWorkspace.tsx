"use client";

import Link from "next/link";
import { usePathname, useRouter } from "next/navigation";
import { useEffect, useRef, useState } from "react";
import { Archive, CalendarDays, FileText, Plus, Save, Search, Upload, Wrench } from "lucide-react";
import type { AuthSession } from "@/lib/auth";
import type { AccountOwnerOption } from "@/lib/client-campaigns";
import type { CampaignDateFilter, CampaignListItem, CampaignOverview, ClientListItem, FinanceSummary, WorkspaceDocument, WorkspacePage } from "@/lib/client-campaign-workspaces";
import type { CrmHandoffProposal } from "@/lib/crm-handoff-contract";
import { hasAnyPermission } from "@/lib/rbac";
import { companyEntities } from "@/lib/company-entities";
import { campaignEffectiveStatusLabel, type CampaignEffectiveStatus } from "@/lib/campaigns/campaign-effective-status";
import {
  Dialog, DocumentUploadDialog, DocumentsList, EmptyState, ErrorState, Feedback, Field, LoadingState,
  OwnerSelect, Pagination, Panel, SelectField, StatusBadge, Tabs, TextArea, WorkspaceHeader,
  dateInput, dateLabel, emptyCampaignForm, moneyLabel, nullable, type CampaignForm
} from "@/components/admin/client-campaigns/WorkspaceUi";

type ReservationRow = { id: string; locationId: string; locationCode: string; locationName: string | null; city: string | null; status: string; periodStart: string; periodEnd: string; installationDate: string | null; neutralizationDate: string | null; monthlyRent: number; currency: string; productionNotes: string | null };
type DetailTab = "overview" | "reservations" | "documents" | "finance";
type SectionData = { reservations?: ReservationRow[]; documents?: WorkspaceDocument[]; finance?: FinanceSummary };
type ExecutiveCampaignContext = {
  entityCode: string | null;
  snapshotDate: string | null;
  dateFilter: CampaignDateFilter | null;
};

export function CampaignsWorkspace({ initialPage, initialEffectiveStatus, initialCampaignId, initialClientId, handoffOpportunityId, openCreate, session, accountOwners, executiveContext }: { initialPage: WorkspacePage<CampaignListItem>; initialEffectiveStatus?: CampaignEffectiveStatus | null; initialCampaignId?: string | null; initialClientId?: string | null; handoffOpportunityId?: string | null; openCreate?: boolean; session: AuthSession; accountOwners: AccountOwnerOption[]; executiveContext?: ExecutiveCampaignContext }) {
  const router = useRouter(); const pathname = usePathname();
  const [page, setPage] = useState(initialPage); const [query, setQuery] = useState(initialPage.query);
  const [effectiveStatus, setEffectiveStatus] = useState<CampaignEffectiveStatus | "">(initialEffectiveStatus || "");
  const [selectedId, setSelectedId] = useState(initialCampaignId || ""); const [overview, setOverview] = useState<CampaignOverview | null>(null);
  const [tab, setTab] = useState<DetailTab>("overview"); const [sections, setSections] = useState<Record<string, SectionData>>({});
  const [loadingList, setLoadingList] = useState(false); const [loadingDetail, setLoadingDetail] = useState(false); const [loadingSection, setLoadingSection] = useState(false);
  const [error, setError] = useState(""); const [message, setMessage] = useState(""); const [cursorTrail, setCursorTrail] = useState<Array<string | null>>([null]);
  const [editorOpen, setEditorOpen] = useState(Boolean(openCreate)); const [editing, setEditing] = useState(false);
  const [form, setForm] = useState<CampaignForm>({ ...emptyCampaignForm, clientId: initialClientId || "", sellerUserId: ["SALES_AGENT", "SALES_DIRECTOR"].includes(session.role) ? session.id : "", accountOwnerUserId: ["SALES_AGENT", "SALES_DIRECTOR"].includes(session.role) ? session.id : "" });
  const [clientQuery, setClientQuery] = useState(""); const [clientOptions, setClientOptions] = useState<ClientListItem[]>([]);
  const [documentOpen, setDocumentOpen] = useState(false); const [operationTarget, setOperationTarget] = useState<ReservationRow | null>(null);
  const [handoff, setHandoff] = useState<CrmHandoffProposal | null>(null); const [handoffBusy, setHandoffBusy] = useState(Boolean(handoffOpportunityId));
  const initialQuery = useRef(initialPage.query);
  const canManage = hasAnyPermission(session.role, ["campaigns.manage", "reservations.manage", "reservations.manage.own"]);
  const canConfirmHandoff = canManage && session.role !== "COO";
  const canChangeOwner = ["COO", "SALES_DIRECTOR", "SUPER_ADMIN"].includes(session.role);
  const selectedSection = sections[selectedId] || {};

  useEffect(() => { if (initialQuery.current === query) { initialQuery.current = "__handled__"; return; } const timer = window.setTimeout(() => { setCursorTrail([null]); void loadPage(null, query); }, 350); return () => window.clearTimeout(timer); }, [query]);
  useEffect(() => { if (!selectedId) { setOverview(null); return; } void loadOverview(selectedId); }, [selectedId]);
  useEffect(() => { if (!selectedId || tab === "overview" || selectedSection[tab]) return; void loadSection(tab); }, [selectedId, tab]);
  useEffect(() => { if (clientQuery.trim().length < 2) { setClientOptions([]); return; } const timer = window.setTimeout(async () => { const response = await fetch(`/api/admin/clients?q=${encodeURIComponent(clientQuery)}&limit=20`, { cache: "no-store" }); const payload = await response.json().catch(() => null); if (response.ok) setClientOptions(payload?.clients || []); }, 300); return () => window.clearTimeout(timer); }, [clientQuery]);
  useEffect(() => {
    if (!editorOpen || !form.clientId || clientOptions.some((client) => client.id === form.clientId)) return;
    let cancelled = false;
    fetch(`/api/admin/clients/${form.clientId}`, { cache: "no-store" })
      .then((response) => response.ok ? response.json() : null)
      .then((payload) => {
        if (cancelled || !payload?.client) return;
        setClientOptions((current) => [{ id: payload.client.id, companyName: payload.client.companyName } as ClientListItem, ...current]);
        setClientQuery((current) => current || payload.client.companyName);
      });
    return () => { cancelled = true; };
  }, [clientOptions, editorOpen, form.clientId]);
  useEffect(() => {
    if (!handoffOpportunityId) return;
    let cancelled = false;
    setHandoffBusy(true);
    fetch(`/api/admin/crm/opportunities/${encodeURIComponent(handoffOpportunityId)}/handoff`, { cache: "no-store" })
      .then(async (response) => {
        const payload = await response.json().catch(() => null);
        if (!response.ok) throw new Error(payload?.error || "Predarea CRM nu a putut fi pregatita.");
        return payload.proposal as CrmHandoffProposal;
      })
      .then((proposal) => {
        if (cancelled) return;
        setHandoff(proposal);
        if (proposal.existingCampaign) {
          setSelectedId(proposal.existingCampaign.id);
          setEditorOpen(false);
          setMessage("Campania exista deja. Am deschis dosarul existent fara a crea un duplicat.");
          return;
        }
        if (!proposal.ready || !canConfirmHandoff) return;
        const clientId = proposal.existingClient?.id || initialClientId || "";
        setForm({
          ...emptyCampaignForm,
          clientId,
          campaignName: proposal.campaign.name,
          sellerUserId: proposal.owner?.id || "",
          accountOwnerUserId: proposal.owner?.id || "",
          startDate: proposal.campaign.startDate || "",
          endDate: proposal.campaign.endDate || "",
          currency: proposal.campaign.currency || "EUR",
          totalContractValue: proposal.campaign.totalContractValue == null ? "" : String(proposal.campaign.totalContractValue)
        });
        setClientQuery(proposal.existingClient?.companyName || proposal.company.name);
        if (proposal.existingClient) setClientOptions([{ id: proposal.existingClient.id, companyName: proposal.existingClient.companyName } as ClientListItem]);
        setEditing(false);
        setEditorOpen(true);
      })
      .catch((cause) => { if (!cancelled) setError(cause instanceof Error ? cause.message : "Predarea CRM nu a putut fi pregatita."); })
      .finally(() => { if (!cancelled) setHandoffBusy(false); });
    return () => { cancelled = true; };
  }, [canConfirmHandoff, handoffOpportunityId, initialClientId]);

  function updateUrl(nextId: string, nextQuery = query, nextStatus: CampaignEffectiveStatus | "" = effectiveStatus) { const params = executiveCampaignParams(executiveContext); if (nextQuery) params.set("q", nextQuery); if (nextStatus) params.set("effectiveStatus", nextStatus); if (nextId) params.set("campaignId", nextId); router.replace(`${pathname}${params.size ? `?${params}` : ""}`, { scroll: false }); }

  async function loadPage(cursor: string | null, search = query, statusFilter: CampaignEffectiveStatus | "" = effectiveStatus) { setLoadingList(true); setError(""); try { const params = executiveCampaignParams(executiveContext); params.set("limit", "30"); if (search.trim()) params.set("q", search.trim()); if (statusFilter) params.set("effectiveStatus", statusFilter); if (cursor) params.set("cursor", cursor); const response = await fetch(`/api/admin/campaigns?${params}`, { cache: "no-store" }); const payload = await response.json().catch(() => null); if (!response.ok) throw new Error(payload?.error || "Lista campaniilor nu a putut fi incarcata."); setPage(payload.page); updateUrl(selectedId, search, statusFilter); } catch (cause) { setError(cause instanceof Error ? cause.message : "Lista campaniilor nu a putut fi incarcata."); } finally { setLoadingList(false); } }
  async function loadOverview(id: string) { setLoadingDetail(true); setError(""); setTab("overview"); setSections({}); try { const response = await fetch(`/api/admin/campaigns/${id}`, { cache: "no-store" }); const payload = await response.json().catch(() => null); if (!response.ok) throw new Error(payload?.error || "Campania nu a putut fi incarcata."); setOverview(payload.campaign); setForm(formFromOverview(payload.campaign)); } catch (cause) { setOverview(null); setError(cause instanceof Error ? cause.message : "Campania nu a putut fi incarcata."); } finally { setLoadingDetail(false); } }
  async function loadSection(section: Exclude<DetailTab, "overview">, force = false) { if (!selectedId || (!force && sections[selectedId]?.[section])) return; setLoadingSection(true); setError(""); try { const response = await fetch(`/api/admin/campaigns/${selectedId}/${section}`, { cache: "no-store" }); const payload = await response.json().catch(() => null); if (!response.ok) throw new Error(payload?.error || "Sectiunea nu a putut fi incarcata."); setSections((current) => ({ ...current, [selectedId]: { ...(current[selectedId] || {}), [section]: payload[section] } })); } catch (cause) { setError(cause instanceof Error ? cause.message : "Sectiunea nu a putut fi incarcata."); } finally { setLoadingSection(false); } }

  async function saveCampaign() { setLoadingDetail(true); setError(""); setMessage(""); try { const wasEditing = editing; const response = await fetch(wasEditing && selectedId ? `/api/admin/campaigns/${selectedId}` : "/api/admin/campaigns", { method: wasEditing && selectedId ? "PATCH" : "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ ...form, campaignCode: nullable(form.campaignCode), sellerUserId: nullable(form.sellerUserId), accountOwnerUserId: nullable(form.accountOwnerUserId), startDate: nullable(form.startDate), endDate: nullable(form.endDate), totalContractValue: form.totalContractValue ? Number(form.totalContractValue.replace(",", ".")) : null, paymentTermDays: form.paymentTermDays ? Number(form.paymentTermDays) : null, notes: nullable(form.notes) }) }); const payload = await response.json().catch(() => null); if (!response.ok) throw new Error(payload?.error || "Campania nu a putut fi salvata."); const id = payload.campaign.id as string; setEditorOpen(false); setEditing(false); setSelectedId(id); updateUrl(id); setMessage(wasEditing ? "Campania a fost actualizata." : "Campania a fost creata."); await loadPage(cursorTrail.at(-1) || null); await loadOverview(id); if (!wasEditing && handoff?.ready && canConfirmHandoff) await confirmCampaignHandoff(id); } catch (cause) { setError(cause instanceof Error ? cause.message : "Campania nu a putut fi salvata."); } finally { setLoadingDetail(false); } }
  async function confirmCampaignHandoff(campaignId: string) { if (!handoff || !handoffOpportunityId) return; setHandoffBusy(true); try { const response = await fetch(`/api/admin/crm/opportunities/${encodeURIComponent(handoffOpportunityId)}/handoff`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ version: handoff.version, targetType: "campaign", targetId: campaignId, idempotencyKey: `crm-handoff-campaign-${handoffOpportunityId}-${campaignId}` }) }); const payload = await response.json().catch(() => null); if (!response.ok) { setError(`Campania este salvata, dar legatura CRM necesita reincercare: ${payload?.error || "eroare de audit"}`); return; } setMessage("Campania a fost confirmata explicit si legatura cu oportunitatea a fost auditata."); } catch (cause) { setError(`Campania este salvata, dar legatura CRM necesita reincercare: ${cause instanceof Error ? cause.message : "eroare de retea"}`); } finally { setHandoffBusy(false); } }
  async function archiveCampaign() { if (!selectedId || !overview) return; setLoadingDetail(true); setError(""); try { const response = await fetch(`/api/admin/campaigns/${selectedId}`, { method: "DELETE" }); const payload = await response.json().catch(() => null); if (!response.ok) throw new Error(payload?.error || "Campania nu a putut fi arhivata."); setSelectedId(""); setOverview(null); updateUrl(""); setMessage("Campania a fost arhivata."); await loadPage(null); } catch (cause) { setError(cause instanceof Error ? cause.message : "Campania nu a putut fi arhivata."); } finally { setLoadingDetail(false); } }

  function openCreateEditor() { setEditing(false); setForm({ ...emptyCampaignForm, clientId: initialClientId || "", sellerUserId: ["SALES_AGENT", "SALES_DIRECTOR"].includes(session.role) ? session.id : "", accountOwnerUserId: ["SALES_AGENT", "SALES_DIRECTOR"].includes(session.role) ? session.id : "" }); setClientQuery(""); setEditorOpen(true); }
  function openEditEditor() { if (!overview) return; setEditing(true); setForm(formFromOverview(overview)); setClientQuery(overview.clientName); setClientOptions([{ id: overview.clientId, companyName: overview.clientName } as ClientListItem]); setEditorOpen(true); }
  const nextPage = () => { if (!page.nextCursor) return; const next = page.nextCursor; setCursorTrail((current) => [...current, next]); void loadPage(next); };
  const previousPage = () => { if (cursorTrail.length <= 1) return; const trail = cursorTrail.slice(0, -1); setCursorTrail(trail); void loadPage(trail.at(-1) || null); };

  return <main className="focus-container grid min-w-0 gap-5 py-6">
    <WorkspaceHeader eyebrow="Portofoliu comercial" title="Campanii" description="Campaniile au propria lista si propriul dosar. Inchirierile, documentele si situatia financiara se incarca numai cand deschizi sectiunea respectiva." actions={<>{canManage ? <button className="focus-button" type="button" onClick={openCreateEditor}><Plus size={17} /> Campanie noua</button> : null}<Link className="focus-button secondary" href="/admin/clienti">Clienti</Link></>} />
    {executiveContext?.snapshotDate || executiveContext?.entityCode || executiveContext?.dateFilter ? (
      <div className="rounded-md border border-focus-yellow/30 bg-focus-yellow/5 px-3 py-2 text-xs text-slate-300">
        Filtru executiv aplicat:
        {executiveContext.entityCode ? ` entitate ${executiveContext.entityCode}` : " toate entitatile"}
        {executiveContext.snapshotDate ? ` · snapshot ${executiveContext.snapshotDate}` : ""}
        {executiveContext.dateFilter === "STARTS_ON" ? " · campanii care incep la snapshot" : ""}
        {executiveContext.dateFilter === "ENDS_ON" ? " · campanii care se incheie la snapshot" : ""}
      </div>
    ) : null}
    {handoffBusy ? <Feedback tone="success">Pregatim datele oportunitatii castigate...</Feedback> : null}
    {handoff ? <Panel title="Predare explicita din CRM" action={handoff.ready && handoff.existingCampaign && canConfirmHandoff ? <button className="focus-button" type="button" disabled={handoffBusy} onClick={() => void confirmCampaignHandoff(handoff.existingCampaign!.id)}><Save size={16} /> Confirma campania existenta</button> : undefined}><p className="text-sm text-slate-300">Campania nu este creata automat. {handoff.existingCampaign ? `Am identificat campania ${handoff.existingCampaign.campaignName}; confirma asocierea fara a crea un duplicat.` : "Verifica clientul, perioada, valoarea integrala si responsabilul, apoi salveaza explicit."}</p>{handoff.warnings.length ? <ul className="mt-2 grid gap-1 text-xs text-amber-200">{handoff.warnings.map((warning) => <li key={warning}>{warning}</li>)}</ul> : null}</Panel> : null}
    {message ? <Feedback tone="success">{message}</Feedback> : null}{error ? <Feedback tone="error">{error}</Feedback> : null}
    <section className="grid min-w-0 gap-5 xl:grid-cols-[360px_minmax(0,1fr)]">
      <Panel title={`Campanii (${page.total})`}><div className="grid gap-2"><label className="relative block"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" size={17} /><input className="focus-input w-full pl-10" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Campanie, client sau cod" /></label><select className="focus-input w-full" aria-label="Filtreaza dupa statusul efectiv" value={effectiveStatus} onChange={(event) => { const value = event.target.value as CampaignEffectiveStatus | ""; setEffectiveStatus(value); setCursorTrail([null]); void loadPage(null, query, value); }}><option value="">Toate statusurile</option>{(["ACTIVE", "SCHEDULED", "ENDED", "DRAFT", "INCOMPLETE", "CANCELLED"] as CampaignEffectiveStatus[]).map((status) => <option key={status} value={status}>{campaignEffectiveStatusLabel(status)}</option>)}</select></div><div className="mt-3 grid max-h-[calc(100vh-18rem)] gap-2 overflow-y-auto pr-1">{loadingList ? <LoadingState text="Actualizam lista..." /> : page.items.length ? page.items.map((campaign) => <button className={`rounded-lg border p-3 text-left ${selectedId === campaign.id ? "border-focus-yellow bg-focus-yellow/10" : "border-focus-line bg-focus-navy/30 hover:border-focus-yellow/50"}`} key={campaign.id} type="button" onClick={() => { setSelectedId(campaign.id); updateUrl(campaign.id); setError(""); setMessage(""); }}><span className="block truncate font-black text-white">{campaign.campaignName}</span><span className="mt-1 block truncate text-xs text-slate-400">{campaign.clientName} / {campaign.sellerName || "Fara agent"}</span><span className="mt-2 flex items-center justify-between gap-2"><StatusBadge value={campaign.effectiveStatus} /><span className="text-xs font-black text-white">{moneyLabel(campaign.totalContractValue, campaign.currency)}</span></span></button>) : <EmptyState>Nu exista campanii pentru cautarea curenta.</EmptyState>}</div><Pagination total={page.total} showing={page.items.length} canPrevious={cursorTrail.length > 1} canNext={Boolean(page.nextCursor)} busy={loadingList} onPrevious={previousPage} onNext={nextPage} /></Panel>
      <div className="min-w-0">{!selectedId ? <EmptyState>Alege o campanie pentru a deschide dosarul.</EmptyState> : loadingDetail ? <Panel><LoadingState text="Deschidem dosarul campaniei..." /></Panel> : overview ? <div className="grid min-w-0 gap-4"><Panel title={overview.campaignName} action={<div className="flex flex-wrap gap-2">{overview.canEdit ? <button className="focus-button secondary" type="button" onClick={openEditEditor}><Save size={16} /> Editeaza</button> : null}<StatusBadge value={overview.effectiveStatus} /></div>}><div className="mb-4 flex flex-wrap gap-3 text-xs text-slate-400"><span>Client: <strong className="text-white">{overview.clientName}</strong></span><span>Agent: <strong className="text-white">{overview.sellerName || "Nesetat"}</strong></span><span>{overview.effectivePeriodSource === "ACTIVE_BOOKING" ? "Perioada activa" : "Perioada"}: <strong className="text-white">{dateLabel(overview.effectiveStartDate)} - {dateLabel(overview.effectiveEndDate)}</strong></span>{overview.effectivePeriodSource === "ACTIVE_BOOKING" && (dateInput(overview.startDate) !== overview.effectiveStartDate || dateInput(overview.endDate) !== overview.effectiveEndDate) ? <span>Date campanie: <strong className="text-amber-200">{dateLabel(overview.startDate)} - {dateLabel(overview.endDate)}</strong></span> : null}</div><Tabs value={tab} onChange={(value) => setTab(value as DetailTab)} items={[{ id: "overview", label: "Rezumat" }, { id: "reservations", label: "Locatii inchiriate" }, { id: "documents", label: "Documente" }, { id: "finance", label: "Financiar" }]} /></Panel>{tab === "overview" ? <OverviewPanel campaign={overview} onArchive={archiveCampaign} busy={loadingDetail} /> : null}{tab !== "overview" && loadingSection ? <Panel><LoadingState /></Panel> : null}{tab === "reservations" && !loadingSection ? <ReservationsPanel rows={selectedSection.reservations || []} canEdit={overview.canEdit} onOperation={setOperationTarget} /> : null}{tab === "documents" && !loadingSection ? <Panel title="Documente campanie" action={overview.canEdit ? <button className="focus-button" type="button" onClick={() => setDocumentOpen(true)}><Upload size={16} /> Incarca</button> : null}><DocumentsList documents={selectedSection.documents || []} /></Panel> : null}{tab === "finance" && !loadingSection ? <FinancePanel finance={selectedSection.finance} campaignId={overview.id} /> : null}</div> : <ErrorState text="Dosarul campaniei nu este disponibil." onRetry={() => loadOverview(selectedId)} />}</div>
    </section>
    {editorOpen ? <CampaignEditorDialog form={form} setForm={setForm} owners={accountOwners} clients={clientOptions} clientQuery={clientQuery} setClientQuery={setClientQuery} canChangeOwner={canChangeOwner} editing={editing} busy={loadingDetail} onClose={() => setEditorOpen(false)} onSave={saveCampaign} /> : null}
    {documentOpen && overview ? <DocumentUploadDialog target={{ campaignId: overview.id, label: overview.campaignName }} onClose={() => setDocumentOpen(false)} onSaved={() => { setDocumentOpen(false); setMessage("Documentul a fost salvat."); void loadSection("documents", true); }} /> : null}
    {operationTarget ? <OperationDialog reservation={operationTarget} onClose={() => setOperationTarget(null)} onSaved={() => { setOperationTarget(null); setMessage("Redecorarea a fost trimisa in operational."); }} /> : null}
  </main>;
}

function OverviewPanel({ campaign, onArchive, busy }: { campaign: CampaignOverview; onArchive: () => void; busy: boolean }) { return <Panel title="Rezumat comercial" action={campaign.canEdit ? <button className="focus-button secondary" type="button" disabled={busy} onClick={onArchive}><Archive size={16} /> Arhiveaza</button> : null}><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Valoare contract" value={moneyLabel(campaign.totalContractValue, campaign.currency)} /><Metric label="Termen plata" value={campaign.paymentTermDays != null ? `${campaign.paymentTermDays} zile` : "Nesetat"} /><Metric label="Facturare" value={campaign.billingRule || "Nesetata"} /><Metric label="Entitate" value={campaign.companyEntity || "Nesetata"} /></div>{campaign.notes ? <p className="mt-4 rounded-lg border border-focus-line bg-focus-navy/25 p-3 text-sm text-slate-300">{campaign.notes}</p> : null}</Panel>; }

function ReservationsPanel({ rows, canEdit, onOperation }: { rows: ReservationRow[]; canEdit: boolean; onOperation: (row: ReservationRow) => void }) { return <Panel title="Locatii inchiriate"><div className="grid gap-2">{rows.length ? rows.map((row) => <article className="flex flex-wrap items-center justify-between gap-3 rounded-lg border border-focus-line bg-focus-navy/30 p-3" key={row.id}><div><strong className="text-white">{row.locationCode}</strong><p className="text-xs text-slate-400">{row.city || row.locationName || "-"} / {dateLabel(row.periodStart)} - {dateLabel(row.periodEnd)}</p></div><div className="flex flex-wrap items-center gap-2"><StatusBadge value={row.status} /><Link className="focus-button secondary" href={`/admin/locatii?reservationId=${row.id}`}>Detalii</Link>{canEdit && row.status === "BOOKED" ? <button className="focus-button secondary" type="button" onClick={() => onOperation(row)}><Wrench size={15} /> Redecorare</button> : null}</div></article>) : <EmptyState>Campania nu are inchirieri.</EmptyState>}</div></Panel>; }

function FinancePanel({ finance, campaignId }: { finance?: FinanceSummary; campaignId: string }) { if (!finance) return <Panel><EmptyState>Nu exista rezumat financiar.</EmptyState></Panel>; return <Panel title="Rezumat financiar" action={<Link className="focus-button secondary" href={`/admin/financiar/incasari?campaignId=${campaignId}`}>Facturi campanie</Link>}><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Facturat RON" value={moneyLabel(finance.invoicedRon, "RON")} /><Metric label="Facturat EUR" value={moneyLabel(finance.invoicedEur, "EUR")} /><Metric label="Sold RON" value={moneyLabel(finance.remainingRon, "RON")} /><Metric label="Sold EUR" value={moneyLabel(finance.remainingEur, "EUR")} /></div><div className="mt-4 grid gap-2">{finance.rows.slice(0, 12).map((row) => <article className="flex flex-wrap items-center justify-between gap-3 rounded-lg border border-focus-line p-3" key={row.id}><span><strong className="text-white">{row.invoiceNumber || "Factura fara numar"}</strong><small className="block text-slate-400">Scadenta {dateLabel(row.dueDate)}</small></span><span className="font-black text-white">{moneyLabel(row.remaining, row.currency)}</span></article>)}</div></Panel>; }

function CampaignEditorDialog({ form, setForm, owners, clients, clientQuery, setClientQuery, canChangeOwner, editing, busy, onClose, onSave }: { form: CampaignForm; setForm: (value: CampaignForm) => void; owners: AccountOwnerOption[]; clients: ClientListItem[]; clientQuery: string; setClientQuery: (value: string) => void; canChangeOwner: boolean; editing: boolean; busy: boolean; onClose: () => void; onSave: () => void }) { return <Dialog title={editing ? "Editeaza campanie" : "Campanie noua"} subtitle="Date comerciale" onClose={onClose}><div className="grid gap-3 md:grid-cols-2"><Field label="Cauta client" value={clientQuery} onChange={setClientQuery} placeholder="Scrie minimum 2 caractere" /><SelectField label="Client" value={form.clientId} onChange={(clientId) => setForm({ ...form, clientId })}><option value="">Alege clientul</option>{clients.map((client) => <option key={client.id} value={client.id}>{client.companyName}</option>)}</SelectField><Field label="Nume campanie" value={form.campaignName} onChange={(campaignName) => setForm({ ...form, campaignName })} /><Field label="Cod campanie" value={form.campaignCode} onChange={(campaignCode) => setForm({ ...form, campaignCode })} /><SelectField label="Status" value={form.status} onChange={(status) => setForm({ ...form, status })}><option value="draft">Ciorna</option><option value="planned">Planificata</option><option value="active">Activa</option><option value="completed">Finalizata</option><option value="cancelled">Anulata</option></SelectField><SelectField label="Entitate contractuala" value={form.companyEntity} onChange={(companyEntity) => setForm({ ...form, companyEntity })}>{companyEntities.map((entity) => <option key={entity.value} value={entity.value}>{entity.label}</option>)}</SelectField><OwnerSelect label="Agent de vanzari" value={form.sellerUserId} owners={owners} disabled={!canChangeOwner} onChange={(sellerUserId) => setForm({ ...form, sellerUserId, accountOwnerUserId: form.accountOwnerUserId || sellerUserId })} /><OwnerSelect label="Responsabil campanie" value={form.accountOwnerUserId} owners={owners} disabled={!canChangeOwner} onChange={(accountOwnerUserId) => setForm({ ...form, accountOwnerUserId })} /><Field label="Start" type="date" value={form.startDate} onChange={(startDate) => setForm({ ...form, startDate })} /><Field label="Final" type="date" value={form.endDate} onChange={(endDate) => setForm({ ...form, endDate })} /><Field label="Valoare contract" value={form.totalContractValue} onChange={(totalContractValue) => setForm({ ...form, totalContractValue })} /><SelectField label="Moneda" value={form.currency} onChange={(currency) => setForm({ ...form, currency })}><option value="EUR">EUR</option><option value="RON">RON</option></SelectField><Field label="Termen plata (zile)" type="number" value={form.paymentTermDays} onChange={(paymentTermDays) => setForm({ ...form, paymentTermDays })} /><SelectField label="Facturare" value={form.billingRule} onChange={(billingRule) => setForm({ ...form, billingRule })}><option value="manual_per_contract">Conform contract</option><option value="campaign_start">La inceputul campaniei</option><option value="monthly_in_advance">Lunar in avans</option><option value="monthly_after_service">Lunar dupa prestare</option></SelectField><div className="md:col-span-2"><TextArea label="Observatii" value={form.notes} onChange={(notes) => setForm({ ...form, notes })} /></div></div><p className="mt-3 text-xs text-slate-400">Campania necesita client si responsabil explicit. Nu se deduce automat un owner din alte inregistrari.</p><div className="mt-4 flex justify-end"><button className="focus-button" type="button" disabled={busy || !form.clientId || !form.campaignName.trim() || !form.sellerUserId || !form.accountOwnerUserId} onClick={onSave}><Save size={16} /> Salveaza</button></div></Dialog>; }

function OperationDialog({ reservation, onClose, onSaved }: { reservation: ReservationRow; onClose: () => void; onSaved: () => void }) { const [date, setDate] = useState(new Date().toISOString().slice(0, 10)); const [cost, setCost] = useState(""); const [note, setNote] = useState(""); const [briefUrl, setBriefUrl] = useState(""); const [busy, setBusy] = useState(false); const [error, setError] = useState(""); async function submit() { setBusy(true); setError(""); try { const response = await fetch(`/api/reservations/${reservation.id}/operations`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ kind: "redecoration", requestedDate: date, cost: cost ? Number(cost.replace(",", ".")) : 0, currency: "RON", costOwner: "client", note: nullable(note), briefUrl: nullable(briefUrl) }) }); const payload = await response.json().catch(() => null); if (!response.ok) throw new Error(payload?.error || "Redecorarea nu a putut fi salvata."); onSaved(); } catch (cause) { setError(cause instanceof Error ? cause.message : "Redecorarea nu a putut fi salvata."); } finally { setBusy(false); } } return <Dialog title="Redecorare" subtitle={reservation.locationCode} onClose={onClose}><div className="grid gap-3 md:grid-cols-2"><Field label="Data solicitata" type="date" value={date} onChange={setDate} /><Field label="Cost estimat RON" value={cost} onChange={setCost} /><Field label="Link brief" value={briefUrl} onChange={setBriefUrl} /><div className="md:col-span-2"><TextArea label="Nota operationala" value={note} onChange={setNote} /></div></div>{error ? <Feedback tone="error">{error}</Feedback> : null}<div className="mt-4 flex justify-end"><button className="focus-button" type="button" disabled={busy || !date} onClick={submit}><Wrench size={16} /> Trimite in operational</button></div></Dialog>; }

function Metric({ label, value }: { label: string; value: string }) { return <article className="rounded-lg border border-focus-line bg-focus-navy/30 p-3"><p className="text-xs font-black uppercase text-slate-400">{label}</p><p className="mt-1 text-xl font-black text-white">{value}</p></article>; }
function formFromOverview(campaign: CampaignOverview): CampaignForm { return { clientId: campaign.clientId, campaignName: campaign.campaignName, campaignCode: campaign.campaignCode || "", status: campaign.status, companyEntity: campaign.companyEntity || "Focus Media", sellerUserId: campaign.sellerUserId || "", accountOwnerUserId: campaign.accountOwnerUserId || "", startDate: dateInput(campaign.startDate), endDate: dateInput(campaign.endDate), currency: campaign.currency || "EUR", totalContractValue: campaign.totalContractValue ? String(campaign.totalContractValue) : "", paymentTermType: campaign.paymentTermType || "30_days", paymentTermDays: campaign.paymentTermDays != null ? String(campaign.paymentTermDays) : "30", billingRule: campaign.billingRule || "manual_per_contract", billingFrequency: campaign.billingFrequency || "monthly", notes: campaign.notes || "" }; }

function executiveCampaignParams(context?: ExecutiveCampaignContext) {
  const params = new URLSearchParams();
  if (context?.entityCode) params.set("entity", context.entityCode);
  if (context?.snapshotDate) params.set("snapshot", context.snapshotDate);
  if (context?.dateFilter) params.set("dateFilter", context.dateFilter);
  return params;
}
